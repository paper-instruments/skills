<!-- Generated from openpyxl 0.2.1 docstrings by griffe2md. Do not edit: fix the docstring in the source. -->

# `openpyxl.preserve.zipguard`

## `LargeZipFile`

```python
LargeZipFile = zipfile.LargeZipFile
```

## `validate_archive`

```python
validate_archive(archive, *, context = 'preserve-mode package')
```

Validate one ZIP before any workbook model is constructed.

## `validate_package_bytes`

```python
validate_package_bytes(data, *, context = 'workbook package')
```
